source "https://rubygems.org"

# Pin this deliberately. Letting the self-hosted runner silently pick up
# "whatever fastlane happens to be installed" is how you get builds that
# pass on Monday and mysteriously fail on Friday after a background `gem update`.
gem "fastlane", "~> 2.236"
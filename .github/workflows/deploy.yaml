name: Deploy iOS App

on:
  workflow_dispatch:
    inputs:
      manual_build_number:
        description: 'Build Number (Optional. Leave empty to auto-increment from TestFlight)'
        required: false
        default: ''

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Verify Environment
        run: |
          echo "Current User:" && whoami
          echo "Xcode Version:" && xcodebuild -version
          export PATH="/opt/homebrew/bin:/usr/local/bin:$PATH"
          which fastlane || echo "Fastlane not found in standard path"

      - name: Deploy to TestFlight
        env:
          APP_STORE_CONNECT_API_KEY_KEY_ID: ${{ secrets.APP_STORE_CONNECT_API_KEY_KEY_ID }}
          APP_STORE_CONNECT_API_KEY_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_API_KEY_ISSUER_ID }}
          APP_STORE_CONNECT_API_KEY_B64: ${{ secrets.APP_STORE_CONNECT_API_KEY_B64 }}
          APP_IDENTIFIER: ${{ secrets.APP_IDENTIFIER }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          TEAM_ID: ${{ secrets.TEAM_ID }}
          ITC_TEAM_ID: ${{ secrets.ITC_TEAM_ID }}
        run: |
          export PATH="/opt/homebrew/bin:/usr/local/bin:$PATH"
          
          # 💡 Pass the optional workflow input value downstream safely as a parameter matrix
          bundle exec fastlane beta build_number:"${{ github.event.inputs.manual_build_number }}" || fastlane beta build_number:"${{ github.event.inputs.manual_build_number }}"
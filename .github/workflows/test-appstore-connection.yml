name: Test App Store Connection

on:
  workflow_dispatch:

jobs:
  test-connection:
    runs-on: macos-14

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Select Xcode 16.2
        run: sudo xcode-select -s /Applications/Xcode_16.2.app

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3'

      - name: Install Fastlane
        run: sudo gem install fastlane -v 2.236.0

      - name: Create App Store Connect API Key
        env:
          APP_STORE_CONNECT_API_KEY_KEY_ID: ${{ secrets.APP_STORE_CONNECT_API_KEY_KEY_ID }}
          APP_STORE_CONNECT_API_KEY_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_API_KEY_ISSUER_ID }}
          APP_STORE_CONNECT_API_KEY_B64: ${{ secrets.APP_STORE_CONNECT_API_KEY_B64 }}
        run: |
          TMPKEY=$(mktemp /tmp/AuthKey.XXXXXX.p8)
          echo "$APP_STORE_CONNECT_API_KEY_B64" | base64 --decode > "$TMPKEY"
          chmod 600 "$TMPKEY"

          cat > api_key.json <<EOF
          {
            "key_id": "$APP_STORE_CONNECT_API_KEY_KEY_ID",
            "issuer_id": "$APP_STORE_CONNECT_API_KEY_ISSUER_ID",
            "key_filepath": "$TMPKEY"
          }
          EOF

      - name: Test App Store Connect Connection
        env:
          APP_IDENTIFIER: ${{ secrets.APP_IDENTIFIER }}
        run: |
          fastlane pilot list \
            --api_key_path api_key.json \
            --app_identifier "$APP_IDENTIFIER" \
            --verbose